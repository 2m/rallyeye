name: Auto approve dependency bump PRs

on: pull_request_target

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    if: github.actor == 'dependabot[bot]' || github.actor == 'scala-steward-2m[bot]'
    steps:
    - uses: hmarr/auto-approve-action@v3
